name: dex_trades
version: "1.0.0"
profile: dex_trades

model-paths: ["models"]
test-paths: ["tests"]

vars:
  dust_token_threshold: 0.000001
  dust_usdc_threshold: 1.0

models:
  dex_trades:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
