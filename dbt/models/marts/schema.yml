version: 2

models:
  - name: mart_dex_trades
    columns:
      - name: chain
        tests: [not_null]
      - name: is_clean
        tests: [not_null]

  - name: mart_dex_volume_by_protocol
    columns:
      - name: trade_count
        tests: [not_null]

  - name: mart_orderflow_signals
    description: Orderflow / MEV-lite proxy rates by chain × protocol (not sandwich proof).
    columns:
      - name: trade_count
        tests: [not_null]
      - name: interesting_trade_rate
        tests: [not_null]
